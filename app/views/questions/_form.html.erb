<%= form_for @question, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :exam_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :exam_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :group_number, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :group_number, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :number, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :number, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :xml, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :xml, :class => 'text_area' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                questions_path, :class => 'btn' %>
  </div>
<% end %>
